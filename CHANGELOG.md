# Changelog

## 0.1.5 — 2026-09-19

### 修复

- **LaTeX 公式渲染鲁棒性**（MPE 兼容）：
  - `$` / `$$` 与公式之间的空格、换行不再导致公式失效（`$ x $`、`$$ x $$`、`$ x$` 均渲染，内容首尾空白自动去除）
  - 宽松形式的行内公式允许跨行（限同一段落内）；表格单元格中的 `$$ ... $$` 块级公式现在可以渲染
  - 单个 `$` / `$$`（无闭合）仍视为普通文本，货币写法不受影响
- **MathJax 扩展本地化**：`\boldsymbol` 等扩展宏此前需联网自动加载，失败会导致全部公式不渲染；现已内置全部 35 个 TeX 扩展文件（`resources/vendor/mathjax/input/tex/extensions/`），离线可用
- `<br>` 标签在表格单元格等处不再显示为字面文本，统一转为换行

### 命名与文档

- 显示名改为 `MPE2PDF — Markdown to PDF`，命令改为 `MPE2PDF: Export to PDF`，`package.json` 描述改为英文
- README 改为英文版（顶部提供中文说明文档链接），中文说明移至 `README.zh-CN.md`

## 0.1.4 — 2026-09-19

### 新增

- 设置 `mpe2pdf.fontSize`（默认 16，范围 8–32）：自定义正文字号（px），标题、代码块、提示框等按比例联动缩放

### 修复

- `mpe2pdf.cssOverride` 此前未实际生效（覆盖样式未注入导出页面），现已修复

### 调整

- 独占段落的图片（含链接包裹）自动居中显示，与 Mermaid/PlantUML 图一致；行文中的行内小图不受影响
- 编辑器右键菜单条目移至 MPE（Markdown Preview Enhanced）同一分组显示

### 文档

- 插件市场描述调整：样式来源统一表述为借鉴 Markdown Preview Enhanced（MPE）
- README 精简：移除安装、开发、Marketplace 发布等与使用者无关的章节

## 0.1.3 — 2026-09-18

### 功能

- 新增设置 `mpe2pdf.headingRule`（默认开启）：可关闭页面主标题与正文二级标题下方的分隔线

## 0.1.2 — 2026-09-18

### 样式

- 标题层级调整：一级标题加大（页面主标题 clamp 上限 2.6→2.9rem，正文 h1 1.9→2.2rem），
  二级及以下标题整体缩小一档（h2 1.75→1.5、h3 1.6→1.35、h4 1.45→1.22、h5 1.3→1.12、h6 1.15→1.05rem），
  拉开原本过于接近的 h1/h2 字号差

## 0.1.1 — 2026-09-18

### 修复

- 无 front matter 的常规 Markdown 导出时，首个 `#` 一级标题在页面标题与正文中重复出现两次的问题（现仅保留页面标题处一次；front matter 标题与正文首个 `#` 相同时同样去重，不同则两者均保留）

## 0.1.0 — 2026-08-06

初始版本，五个阶段全部完成。

### 功能

- **MPE 风格渲染**：以 Markdown Preview Enhanced（MPE）风格渲染 Markdown
- **13 种 MPE 风格提示框**：`[!note]` `[!info]` `[!tip]` `[!success]` `[!question]`
  `[!warning]` `[!example]` `[!quote]` `[!important]` `[!bug]` `[!fail]`
- **公式**：`$..$` 行内 / `$$..$$` 块级（MathJax 3，修复了段落中 `$$` 被拆解的问题）
- **图表**：Mermaid（客户端渲染，跟随明暗主题）、PlantUML（Java 渲染，未配置时降级显示源码）
- **代码高亮**：highlight.js GitHub 明暗主题
- **PDF 导出**：puppeteer-core 驱动系统 Edge/Chrome（零下载），A4/Letter、页边距、页码可配
- **打印排版**：代码块/表格/提示框/公式不跨页截断，标题不孤立页底
- **11 项设置**：主题、纸张、边距、目录、页码、CSS 覆盖、浏览器/Java 路径、输出目录、根路径映射
- **体验**：未保存更改提示、输出目录自动创建、同名文件自动改名不覆盖、完成后一键打开

### 修复

- `$$` 块级公式在段落中/段落后被渲染为行内公式并残留美元符的问题
- MathJax 3 手动/自动 typeset 并发竞态导致的公式偶发不渲染

### 文档

- README：功能、安装、使用、设置表、样式同步指南
- MIT LICENSE

### 工程

- 70 个单元测试（node:test）与渲染产物逐块比对脚本
- 打包体积优化：排除开发目录（29 MB → 6.26 MB）
